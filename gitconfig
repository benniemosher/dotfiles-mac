[user]
	name = Bennie Mosher
	email = bennie.mosher@cpsagu.com
	signingkey = 6D1B03A41016E753
[alias]
	s = status
	co = checkout
	cp = cherry-pick
[credential]
	helper = cache --timeout=3600
[core]
	editor = vim
	excludesfile = ~/.gitignore_global
[commit]
	gpgsign = true
[push]
	default = current
[pull]
	default = current
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[gpg]
	program = /usr/local/bin/gpg
